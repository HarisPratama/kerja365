import React from 'react';
import { View, Text, TouchableOpacity } from 'react-native';

const Header = () => {
    return (
        <TouchableOpacity>
            <View>Header</View>
        </TouchableOpacity>
    );
};

export default Header;
